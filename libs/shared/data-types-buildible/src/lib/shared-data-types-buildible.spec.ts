import { sharedDataTypesBuildible } from './shared-data-types-buildible';

describe('sharedDataTypesBuildible', () => {
  it('should work', () => {
    expect(sharedDataTypesBuildible()).toEqual('shared-data-types-buildible');
  });
});
